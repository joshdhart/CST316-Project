@model IEnumerable<MusicLibrary.Models.AlbumArt>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.url)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Album.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.url)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Album.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.albumArtID }) |
            @Html.ActionLink("Details", "Details", new { id=item.albumArtID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.albumArtID })
        </td>
    </tr>
}

</table>
